"use strict";

var mocha = require("mocha");
var expect = require("chai").expect;

var _require = require("../index"),
    app = _require.app;

describe("All the test for the todolist", function () {
    it("", function () {
        expect(app).to.exist;
    });
});